{
  "name": "mobject-graph-server",
  "version": "1.0.0",
  "description": "use of mobject-graph on a webserver",
  "main": "index.js",
  "scripts": {
    "start": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mobject dev team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.1",
    "mobject-client": "^1.0.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "nodemon": "^3.1.0"
  }
}
